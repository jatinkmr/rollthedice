<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <div class="container">
        <h1>
            <%= title %>
        </h1>

        <div class="prizes-showcase">
            <h2>🏆 Available Prizes</h2>
            <div class="prizes-grid">
                <% Object.keys(allPrizes).forEach(function(diceNumber) { %>
                    <div class="prize-card">
                        <div class="dice-number">🎲 <%= diceNumber %>
                        </div>
                        <div class="prize-name">
                            <%= allPrizes[diceNumber].name %>
                        </div>
                        <div class="prize-description">
                            <%= allPrizes[diceNumber].description %>
                        </div>
                        <div class="prize-value">
                            <%= allPrizes[diceNumber].value %> points
                        </div>
                    </div>
                    <% }); %>
            </div>
        </div>

        <div class="dice-container">
            <div class="dice" id="dice">
                <span class="dice-dots" id="dice-dots">?</span>
            </div>
        </div>

        <button class="roll-btn" id="roll-btn" onclick="rollDice()">🎲 Roll the Dice!</button>

        <div class="prize-section" id="prize-section" style="display: none;">
            <div class="prize-name" id="prize-name"></div>
            <div class="prize-description" id="prize-description"></div>
            <div class="prize-value" id="prize-value"></div>
        </div>

        <div class="rolling-message" id="rolling-message" style="display: none;">
            <p>🎲 Rolling the dice...</p>
        </div>

        <div class="instructions">
            <p>Click the button to roll the dice and win amazing prizes!</p>
            <p>Higher numbers = Better prizes!</p>
        </div>
    </div>

    <script src="/js/dice.js"></script>
</body>

</html>
